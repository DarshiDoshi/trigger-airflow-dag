# trigger-airflow-dag
A powerful CLI tool to trigger and monitor the Airflow DAGs.

## Example Usages:
```
python airflow_dag_trigger.py \
    --url airflow.example.com \
    --username <user_mail-id> \
    --password <user_password> \
    --dag <dag_name> \
```
